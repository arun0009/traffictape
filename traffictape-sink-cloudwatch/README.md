# traffictape-sink-cloudwatch

JSON-line transport when S3 is blocked. Dump the log group back to JSONL before `generate`. Includes `traffictape-spring-boot`. See [configuration](../docs/configuration.md#fargate--cloudwatch).
