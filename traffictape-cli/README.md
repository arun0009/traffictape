# traffictape-cli

Turns a captured corpus into WireMock stubs (default), optional Mountebank imposters, a `test-plan.json`, and a JUnit replay skeleton.

Not a build dependency. Download the self-contained `-all` jar from the [latest release](https://github.com/arun0009/traffictape/releases/latest). See [Generate](../docs/generate.md).
