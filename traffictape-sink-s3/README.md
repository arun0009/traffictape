# traffictape-sink-s3

Same corpus tree as the file sink, in a bucket. Includes `traffictape-spring-boot` — add this instead of the starter. See [configuration](../docs/configuration.md#fargate--s3).
