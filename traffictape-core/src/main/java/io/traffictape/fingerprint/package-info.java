/**
 * Endpoint and scenario identity. Replace {@link io.traffictape.fingerprint.Fingerprinter}
 * with a {@code @Bean}; adapters never call this package directly.
 */
package io.traffictape.fingerprint;
