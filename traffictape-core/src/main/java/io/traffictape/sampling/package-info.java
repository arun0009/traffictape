/**
 * Which scenarios keep example bodies. Replace {@link io.traffictape.sampling.Sampler}
 * with a {@code @Bean}; statistics still record every accepted observation.
 */
package io.traffictape.sampling;
